@model gym_management_system_front_end.Models.SubscriptionTiersViewModel

@{
    ViewData["Title"] = "Delete";
}

<div class="container d-flex justify-content-center align-items-center mt-5" style="min-height: 87vh;">
    <div class="col-md-6 bg-dark rounded p-4 border border-1 border-primary rounded">
        <h2 class="text-white">Delete Subscription Tier</h2>
        <h3 class="text-white">Are you sure you want to delete this?</h3>
        <hr />

        <dl class="row text-white">
            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.SubscriptionTierID)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.SubscriptionTierID)</dd>

            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Name)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Name)</dd>

            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Price)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Price)</dd>

            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Length)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Length)</dd>
        </dl>

        <form asp-action="Delete" asp-route-SubscriptionTierID="@Model.SubscriptionTierID" class="text-center m-1 d-flex justify-content-center">
            
                <button type="submit" value="Delete" class="btn btn-danger m-2">
                   
                    Delete  </button>
            <a class="btn btn-secondary m-2" asp-action="Index">Back to List</a>
            
        </form>

        
    </div>
</div>
